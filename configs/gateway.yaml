server:
  port: 8080
  read_timeout: 30s
  write_timeout: 120s

providers:
  openai:
    base_url: "https://api.openai.com/v1"
    timeout: 30s
    models:
      - gpt-4o
      - gpt-4o-mini
      - gpt-3.5-turbo

log:
  level: info
  format: json
  debug_bodies: false
